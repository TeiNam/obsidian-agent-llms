# Changelog

이 프로젝트의 주요 변경 사항을 기록합니다.
형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르며,
버전 규칙은 [Semantic Versioning](https://semver.org/lang/ko/)을 따릅니다.

> **에디션 안내**: 이 프로젝트는 **AWS Bedrock · Google Gemini · OpenAI · Ollama** 멀티프로바이더 백엔드를 지원합니다.
> 과거 `kiro-edition` 브랜치(Assistant Kiro)는 0.3.0에서 main으로 통합되었습니다.

## [0.7.10] - 2026-09-09

### Fixed

- 0.7.9 배포 후 확인된 GitHub Actions의 Node.js 20 폐기 경고를 정리했다. checkout·setup-node·attestation·release Actions를 Node.js 24 기반 버전으로 갱신하고 CI·릴리스 검증용 Node.js도 24로 맞췄다.
- 플러그인 최소 지원 버전은 Obsidian 1.7.2를 유지한다.

## [0.7.9] - 2026-09-09

### Fixed

- 자격증명 암호화·쓰기·파일 교체 실패 시 기존 로컬 파일을 보존한다. 로컬 저장 성공을 확인한 뒤에만 이전 `data.json`의 키를 제거하며, 실패하면 안내한다. 새 파일은 권한 `0600`의 임시 파일을 완성한 뒤 교체한다.
- MCP 초기화 중 중지·설정 교체가 진행 중인 연결까지 취소한다. 종료 신호 전송 여부가 아닌 실제 종료 상태를 확인해, 3초 뒤에도 살아 있는 직접 자식 프로세스를 강제 종료한다.
- 클립보드 쓰기 완료 후에만 복사 성공 표시를 보여주며, 권한 거부나 API 사용 불가 시 실패를 안내한다.
- Node 타입을 명시하고 불필요한 타입 단언을 제거했다. 첨부·변경 원장의 Base64 변환은 Obsidian 공식 API를 사용한다.

### Changed

- 설정 항목을 실제 `getSettingDefinitions()` 정의로 등록하여 Obsidian 1.13+ 설정 검색을 지원한다. 정의 수집은 DOM·네트워크·저장을 실행하지 않는다. 구버전 `display()`도 같은 정의를 렌더하며 최소 지원 버전은 **1.7.2**를 유지한다.
- MCP 자식 프로세스에는 실행 경로·로케일·임시 폴더 등 허용한 환경 변수만 상속한다. 필요한 서버별 값은 `env`에 명시해야 한다. 실행 파일 탐색은 `spawn`의 `PATH` 처리를 사용한다.
- 기존 도구 확인 옵션을 켜면 모든 MCP 도구 호출에도 승인 모달을 표시한다. 옵션의 기본값은 꺼짐을 유지한다.
- Obsidian API 타입을 1.13.1로 고정하고, AWS SDK 하위 패키지·Vitest·esbuild 등 취약 의존성을 갱신했다. 테스트 설정을 ESM으로 전환했다.
- PR·릴리스 CI에서 오류·경고 없는 lint를 요구한다. 자동 버전 증가 시 `package-lock.json`도 갱신하고, 확정된 버전으로 빌드한다.
- 세 언어 README의 배지와 시스템·네트워크 접근 설명을 갱신했다. [수정 리뷰와 검증 범위](docs/review-0.7.9.md)를 추가했다.

## [0.7.0] - 2026-09-03

### Added

- **이미지·문서 첨부가 네 백엔드에서 동작한다.** 0.5.0에서는 Bedrock 외 백엔드에서 조용한 유실을 막기 위해 거절만 했다. 이제 Gemini는 inlineData(이미지·PDF), OpenAI는 image_url 데이터 URL(이미지), Ollama는 메시지 레벨 images 배열(이미지)로 실제 전달한다. 지원 범위는 백엔드×형식 단위로 판정하며, 거절 안내가 어느 백엔드로 바꾸면 되는지 알려준다.
- **절 단위 인용** (인덱스 스키마 v2). 청크가 어느 헤딩에서 왔는지 기록해 검색 결과에 `[[노트#헤딩]]` 형태를 함께 싣는다. 인용 검증도 앵커까지 확인한다 — 존재하는 노트의 존재하지 않는 절을 인용하는 것도 헛걸음이다. v1 인덱스는 재인덱싱 없이 로드되고 해당 청크만 이 기능이 비활성된다.
- **중복 후보 검토** (`중복 후보 검토` 커맨드). 같은 대상을 다루는 노트 군집을 찾아 정본과 별칭을 제안한다. 아무것도 지우거나 합치지 않는다 — 정본의 `aliases`에 중복 제목을 추가하고 후보 목록을 기록하는 것까지다. LLM 호출 없음.
- **결정 원장** (`결정 추출 → 원장` 커맨드). 흩어진 결정을 한 노트에 모으고 뒤집힌 것은 대체 관계로 표시한다. "왜 X를 선택했나", "아직 열린 약속은?"에 답할 수 있다. 출처 없는 결정은 받지 않으며, 원장에서 직접 고친 값은 다음 추출이 되돌리지 않는다.
- **Inbox 검토** (`Inbox 검토` 커맨드). 지정 폴더의 새 캡처 노트에 제목·이동·태그를 제안한다. 한 번에 12건까지. 실재하지 않는 폴더 제안과 볼트 밖 경로는 걸러내고, 대상 이름이 이미 있으면 건너뛴다. 이동은 `fileManager.renameFile`로 해서 링크가 자동 갱신된다.

### Changed

- 승인 목록 모달(모순 검토·링크 제안·중복 후보·결정·Inbox)이 공통 골격을 쓴다. 옵트인 기본값, 선택 개수 표시, 실패 시 선택 유지가 한 곳에서 정해진다.
- LLM JSON 응답 파싱을 `llm-json.ts`로 모았다. "빈 배열(결과 없음)"과 "해석 실패(잘린 응답)"를 구분하는 의미론이 기능마다 갈리지 않게 한다.

## [0.6.0] - 2026-09-03

### Added

- **모순 검토·반영 승인 화면.** `applyReconciliation`은 구현과 테스트가 처음부터 있었지만 승인 화면이 없어 도달할 수 없었다. 모순별로 대상 노트·상충 진술·제안 정정안을 보여주고 승인한 항목만 노트에 반영한다. 생성 영역만 교체하므로 직접 쓴 내용은 보존된다.
- **링크 제안** (`링크 제안 (고아·스텁 노트 연결)` 커맨드). 고아·스텁 노트에 붙일 링크 후보를 인덱스의 임베딩으로 계산해 승인받는다. LLM 호출이 없다. 유사도 0.82 이상, 노트당 3개까지만 제안하며 승인한 것만 적용한다 — 잘못된 링크는 그래프 오염으로 남아 이후 모든 검색에 영향을 준다.
- **Bedrock 프롬프트 캐싱.** 시스템 프롬프트를 안정 접두어와 매 요청 변하는 시각 블록으로 나눠 그 사이에 캐시 경계를 둔다. 0.4.0에서 시각 블록을 맨 끝에 배치한 이유가 이것이었다. 지원 모델(claude 4+ / gpt-5+ / nova)이고 접두어가 충분히 길 때만 마커를 붙인다 — 미지원 모델에 보내면 요청 전체가 실패한다.

### Changed

- `runReconcileDetailed`가 리포트 문자열과 파싱된 모순 목록을 함께 돌려준다. 기존 `runReconcile`은 얇은 래퍼로 남아 동작이 같다.

## [0.5.0] - 2026-09-03

### Added

- **하이브리드 검색.** 어휘 검색을 벡터 검색과 RRF로 융합한다. 이전에는 어휘 검색이 "임베딩이 아예 없을 때"의 폴백으로만 돌아, dense가 엉뚱한 결과를 낼 때는 쓰이지 않았다. 에러 코드·함수명·버전 문자열·사람 이름처럼 표기가 특이한 문자열에서 정답 노트가 상위권 밖으로 밀리거나 관련도 임계값에서 탈락하던 문제가 사라진다. 어휘 일치가 없는 질의는 기존 순서가 그대로 유지된다.
- **`search_vault` 필터** — `folder`, `tags`, `modifiedAfter`, `modifiedBefore`. "지난달 회의록", "Projects 폴더", "#urgent 태그" 같은 요청을 모델이 조건으로 표현할 수 있다. 임베딩 비교 전 프리필터라 더 빠르기도 하다.
- **인용 검증.** 응답이 밝힌 노트 경로를 볼트와 대조해, 찾을 수 없는 인용을 응답 하단에 경고로 표시한다. 코드블록 안의 예시는 세지 않는다.
- **검색 품질 지표** (`retrieval-metrics.ts`) — recall@k, precision@k, reciprocal rank. 랭킹 변경을 감이 아니라 계측으로 판단하기 위한 개발용 하네스다.
- **자동 스케줄러 주기 실행.** 30분마다 실행 조건을 확인한다. 이전에는 앱 시작 시 한 번만 확인해, 옵시디언을 며칠 열어두면 24시간 주기가 와도 돌지 않았다.

### Fixed

- **바이너리 첨부가 조용히 사라지던 문제.** 이미지·PDF·Office 첨부는 Bedrock Converse 규격 블록으로 만들어지는데, Gemini·OpenAI·Ollama의 변환기는 text·tool_use·tool_result만 인식해 그 블록을 버렸다. 알림도 오류도 없어서 사용자는 모델이 첨부를 보고 답했다고 오해했다. 이제 해당 백엔드에서는 첨부 시점에 이유를 알리고 거절하며, 첨부 후 백엔드를 전환한 경우 전송 시점에도 확인한다.
- 전 단계가 실패한 스케줄러 실행에 1시간 냉각을 건다. 실패는 `lastScheduledRun`을 남기지 않아 즉시 재시도되는데(의도된 설계), 주기 tick과 겹치면 원인이 지속되는 실패에서 30분마다 영원히 재시도하게 된다.

### Changed

- `minAppVersion` 1.5.8 유지. 새로 쓴 API는 없다.

## [0.4.0] - 2026-09-03

### Breaking

- **플러그인 ID를 `ai-assistant` → `agent-llms`로, 표시 이름을 `AI Assistant` → `Agent LLMs`로 변경.** 구 ID는 옵시디언 커뮤니티 플러그인 레지스트리에 `qgrail/obsidian-ai-assistant`가 이미 선점한 값이었다. 옵시디언 업데이터는 설치된 플러그인 폴더명을 레지스트리 ID와 대조하므로, 이 플러그인 폴더를 그쪽 릴리스로 덮어써버렸다. 폴더가 바뀌므로 재설치가 필요하고(BRAT은 항목 제거 후 재추가), 사이드바를 한 번 다시 열어야 한다.
- `viewType`이 `ai-assistant-view` → `agent-llms-view`로 바뀌었다. 워크스페이스 레이아웃에 남은 구 뷰는 빈 탭이 되므로 닫고 다시 열면 된다.
- GitHub 레포와 npm 패키지 이름을 `obsidian-ai-assistant` → `obsidian-agent-llms`로 변경. 릴리스 태그 접두어도 `agent-llms-`로 바뀐다.
- `minAppVersion`을 1.4.0 → 1.5.8로 올렸다. 모달 타이틀을 `Modal.setTitle()`로 옮기면서 필요해졌다.
- 설정 탭 최상단의 "(백엔드) Assistant 설정" 헤딩을 제거했다. 옵시디언이 설정 탭을 이미 플러그인 이름 아래에 렌더하므로 심사 기준이 이 헤딩을 금지한다.

### Added

- **모델이 현재 날짜·시각을 안다.** 시스템 프롬프트 끝에 사용자 로컬 기준 날짜·요일·시각·타임존을 매 요청마다 붙인다. "어제 노트", "이번 주 금요일" 같은 상대 표현을 학습 시점 기준으로 잘못 푸는 문제가 사라진다. `time` MCP 서버가 필요 없고, 네 백엔드(Bedrock·Gemini·OpenAI·Ollama) 모두에 적용된다. 날짜 포맷은 To-Do·회고 노트 경로와 같은 `YYYY-MM-DD`이므로 모델이 경로를 추측하지 않는다.
- `ai-assistant`를 `LEGACY_PLUGIN_IDS`에 추가해 0.3.x의 설정(`data.json`), 볼트 데이터, MCP 설정, 자격증명을 첫 실행 시 자동 복사. 원본은 보존하므로 이전 버전으로 되돌려도 동작한다.
- `pluginId`가 매니페스트 `id`·볼트 데이터 파일명·자격증명 파일명과 어긋나는지 검증하는 테스트(`branding.test.ts`의 "pluginId 결합 검증"). 세 곳 중 하나만 바꾸면 마이그레이션이 조용히 엉뚱한 경로로 복사하던 위험을 막는다.
- 복습 큐 모달이 언어 설정을 따른다. 제목·설명·선정 이유가 en/ko/ja로 나온다(이전에는 한국어 고정).

### Fixed

- **Activity_Log append가 겹치면 줄이 사라지던 문제.** `appendActivityLog`가 read→modify 왕복이라, 스케줄러와 사용자 동작이 동시에 append하면 나중 쓰기가 먼저 읽은 내용을 덮어썼다. 원자적 `vault.process()`로 교체했다.

### Changed

- 커뮤니티 심사 기준 대응: 모달 타이틀 9곳을 `Modal.setTitle()`로, 정적 인라인 스타일 8곳을 CSS 클래스로, `display` 토글 9곳을 `hide()`/`show()`/`toggle()`로 옮겼다. 스트리밍 때문에 `requestUrl` 대신 `fetch`를 쓰는 3곳에는 사유 주석을 달았다.
- `ReviewItem`이 완성된 문구(`reason`) 대신 원자료(`basis`/`elapsedDays`/`links`)를 싣는다. 점수 계산 순수 함수가 로케일을 알 필요가 없어졌고, 문구 조립은 뷰가 담당한다.

### Removed

- **기본 MCP 설정 템플릿에서 `time` 서버를 뺐다.** 시스템 프롬프트가 매 요청마다 로컬 날짜·시각을 실어주므로, 같은 정보를 얻으려고 컨테이너를 띄우고 도구 호출 왕복을 더할 이유가 없다. 이미 저장한 설정은 그대로 유지되며, 직접 지우면 된다.
- 참조되지 않는 CSS 92줄 삭제 — 회고 모달(`.ba-retro-*` 9개 규칙)과 `.ba-thinking-hint`, `.ba-action-btn-label`, `.ba-model-check`, `.ba-model-manual-input`. 클래스 168개 중 미참조는 옵시디언 내장(`svg-icon`, `theme-light`)만 남았다.

### Migration

마이그레이션 완료 알림이 뜬 뒤 `.obsidian/plugins/ai-assistant/` 폴더를 삭제할 것. 덮어쓰기 업데이트가 이미 실행됐다면 그 폴더에는 이 플러그인이 아니라 다른 플러그인의 코드가 들어 있다.

## [0.3.0] - 2026-08-01

### Breaking

- 플러그인 ID를 `bedrock-assistant` → `ai-assistant`로 변경. 플러그인 폴더가 바뀌므로 재설치가 필요하고, 사이드바를 한 번 다시 열어야 한다.
- `kiro-edition` 브랜치 폐기. Bedrock 전용 에디션은 main으로 통합되었고, `assistant-kiro` 데이터도 자동 마이그레이션 대상이다.
- **Bedrock 인증을 API 키 단독으로 축소.** 액세스 키(장기 자격증명 위험)와 `~/.aws` 프로필(기기마다 `aws sso login` + 8~12시간 주기 재로그인)을 모두 제거했다. 기존 사용자는 설정에서 Bedrock API 키를 입력해야 한다. 프로필 인증 코드는 태그 `aws-profile-credential-process`에 보존했다.

### Added

- 구 플러그인 ID(`bedrock-assistant`, `assistant-kiro`)의 설정(`data.json`), 볼트 데이터, MCP 설정, 자격증명을 첫 실행 시 새 경로로 자동 복사. 원본은 보존하므로 이전 버전으로 되돌려도 동작한다.
- 추천 플러그인(Code Styler, Tasks) 설치 여부를 감지해, 이미 설치한 경우 설치 버튼 대신 "설치됨" 배지를 표시.

### Changed

- 임베딩 API를 제공하지 않는 벤더는 백엔드로 지원하지 않는다는 정책을 문서에 기록.

### Removed

- 참조되지 않던 `gemini-icon.svg`, `kiro-icon.svg` 삭제 (아이콘 SVG는 `branding.ts`에 인라인).
- `.gitattributes`의 `branding.ts merge=ours` 삭제 — 단일 브랜치에서는 정상적인 머지 변경을 조용히 버리는 함정이 된다.

## [Unreleased]

### Added
- **누적 학습 기능** — 사용해 갈수록 축적되는 4가지 기능:
  - **회고 체인** — 일일 회고 생성 시 최근 7일의 회고 섹션을 함께 입력해 반복 문제와 개선 여부를 추적합니다. LLM 호출 수는 늘지 않습니다(입력 토큰만 증가).
  - **대화 결론 수확** — 저장된 대화에서 결론·결정·근거·미해결 질문만 추출해 `{Wiki폴더}/Conversations/`에 노트로 남깁니다. 세션은 50개 상한으로 오래된 것이 사라지므로, 남길 가치가 있는 결론을 볼트로 옮깁니다. 원본 대화는 넣지 않습니다.
  - **지식 공백 리포트** — 인덱스 데이터로 구조적 공백(참조되지만 없는 노트, 참조받는 빈 노트, 고립 노트, 단방향 링크)을 계산해 상위 20건을 기록합니다. **LLM 호출 0회.**
  - **복습 큐** — 오래 열지 않았지만 연결이 많은 노트 5건을 제시합니다. 노트에 등급 필드를 심지 않고 열람 이력은 플러그인 설정에만 저장합니다. **LLM 호출 0회.**
- Bedrock 인증 — **Bedrock API 키**(장기 베어러 토큰) 단독. 키가 비어 있으면 SDK 기본 자격증명 체인으로 폴백하지 않고 실패하며, authSchemePreference를 고정해 환경에 남은 ambient 베어러 토큰이 이를 우회하지 못하게 합니다.
- Bedrock 설정 가이드 — `docs/bedrock-setup-kr.md`에 API 키 발급, 모델 접근 활성화, 여러 기기 사용법, 오류별 문제 해결을 정리했습니다.
- Graph RAG 문서 — `docs/second-brain-{en,kr,ja}.md`에 청킹·순회·점수 계산, 도구별 LLM 호출 비용, sentinel 블록 동작 원리를 정리했습니다.
- **멀티프로바이더 AI 백엔드** — 기존 Bedrock/Gemini에 더해 **OpenAI, Ollama** 지원. 설정에서 백엔드를 전환하면 자격증명·모델 목록·아이콘이 함께 바뀝니다.
- 백엔드별 채팅/임베딩 모델 목록 조회 및 모델 큐레이션(Gemini 3.x 채팅 모델, OpenAI gpt-5.x 등).
- 시스템 프롬프트 구조: 내장 기본 프롬프트에 사용자 추가 지침과 스킬을 결합해 주입.
- 스킬 시스템: 내장 스킬(항상 적용) + 사용자 커스텀 스킬(토글 활성화, AI 생성 보조).
- 웹 검색 토글: Gemini 네이티브(Google Search) 또는 fetch/exa/brave MCP를 이용한 채팅 웹 검색.
- 기본 MCP 설정 템플릿(fetch/brave-search/exa/time) 및 MCP 도구 타임아웃 설정(1~60초).
- Graph RAG 기반 볼트 검색: 임베딩 + 그래프 순회로 관련 노트 탐색.
- 백엔드 전환 시 리본/뷰 헤더 아이콘 실시간 갱신.
- 입력 보정: `maxTokens` 허용 범위 클램프, 임베딩 모델/백엔드 변경 시 재인덱싱 안내.

### Fixed
- **자격증명이 비었을 때 의도하지 않은 AWS 계정으로 호출이 나가던 문제** — 키가 비어 있으면 AWS SDK가 기본 자격증명 체인으로 폴백해 `~/.aws/credentials`의 `[default]` 프로필이나 환경변수·IAM 역할을 집어갔습니다. 볼트 인덱싱은 자동으로 대량 호출하므로 사용자가 알아차리기 전에 번집니다. 이제 fail-closed로 명시적으로 실패합니다.
- **ambient 베어러 토큰이 fail-closed를 우회하던 문제** — AWS SDK는 환경변수 `AWS_BEARER_TOKEN_BEDROCK`이 있으면 인증 스킴을 베어러로 자동 승격시키고 `credentials` 공급자를 아예 호출하지 않습니다. 거부 공급자를 넣어도 환경에 남은 다른 계정 토큰으로 요청이 나갔습니다. `authSchemePreference`를 명시적으로 고정해 막았습니다.
- **폐기된 액세스 키 필드가 동기화 대상 `data.json`에 남던 문제** — 민감 필드 목록에서 제거하면서 strip 대상에서도 빠져, 구 설정의 평문 키가 계속 재저장됐습니다. 읽지는 않되 키 자체를 삭제합니다.
- **임베딩 차원 불일치가 조용한 오답을 만들던 문제** — 임베딩 모델을 바꾸면 차원이 달라지는데 유사도를 0으로 계산했고, 정규화를 거쳐 0.5점이 되어 전 노트가 동점이 됐습니다. 결과적으로 경로 알파벳순 상위 N건이 "관련 노트"로 반환됐습니다. 이제 "비교 불가"로 처리해 키워드 검색으로 폴백하고 재인덱싱을 안내합니다.
- 최소 관련성 임계값(0.55) 도입 — 코사인 0(직교=무관)이 정규화 후 0.5로 매핑되어 무관한 노트가 "50% 관련"으로 노출되던 문제를 막습니다. 임계값은 감쇠 전 관련성에 적용됩니다.
- 그래프 이웃 점수에 이웃 자신의 벡터 유사도를 반영 — 기존에는 시드 점수를 그대로 상속해 같은 시드의 무관한 이웃과 관련된 이웃이 동점이었습니다.
- 인덱스 정합성 — 노트 생성·이름변경 이벤트 처리 추가(신규 노트가 전체 재인덱싱까지 검색되지 않고, 이동한 노트의 구 경로가 영구 잔존했음), 비워진 노트의 엔트리 제거(지운 내용이 계속 검색됨), 임베딩 완료 후가 아닌 본문 읽기 시점의 수정 시각 기록(TOCTOU), 임베딩 실패 엔트리의 재시도 표시.
- 인덱싱 요청 직렬화 — 폴더를 일괄 이동하면 파일 수만큼 임베딩 요청이 병렬로 나가 API 쓰로틀링을 맞던 문제를 막습니다.
- Wiki 쓰기 안전성 — 위키 폴더 밖 경로(`..`·절대경로) 쓰기 차단, LLM 출력에 섞인 sentinel 마커 무해화, Second Brain 쓰기 도구 4종을 확인 모달 대상에 추가, `edit_note`의 빈 검색어 거부(노트가 파괴되던 문제).
- P.A.R.A 자동 분류 — 분류 실패 시 무조건 `resources`로 이동시켜 폴더 구조를 임의 재배치하던 동작을 제거하고, LLM 호출 수에 예산을 적용해 재실행 시 이전에 처리되지 않은 파일이 진행되도록 했습니다.
- 스케줄러 — 인덱스 로드 완료를 기다린 뒤 실행(빈 인덱스로 카탈로그를 덮어쓸 수 있었음), 전 단계 실패 시 완료 시각을 갱신하지 않아 다음 트리거에서 재시도.
- 문서 정정 — 존재하지 않는 기능 안내를 제거했습니다(회고 툴바 버튼, Dataview·Templater 스킬, 임베딩 모델 기본값, 웹 클리퍼 프론트매터 "언어" 필드, 설정의 "사용자 경험" 섹션(실제로는 렌더되지 않음), 스케줄러 "24시간마다 자동 실행").

### Changed
- Daily Planner 구조 개편 — TimeBox 제거, 평면 To-Do 폴더 구조(`{폴더}/YYYY-MM-DD To-Do.md`)로 전환, To-Do 템플릿 정비.
- 설정 화면 재구성 — 모양·대화·볼트 관리 3개 섹션으로 분류, 채팅 폰트 크기·MCP 타임아웃을 숫자 입력으로 변경, 추천 플러그인 설치 항목을 하단으로 이동.
- **`temperature` 제거, `effort`(추론 강도)로 대체** — 설정에서 temperature를 없애고 공급자별 스펙에 맞는 파라미터로 전송합니다(OpenAI `reasoning_effort`, Anthropic `output_config.effort`, Gemini `thinkingConfig.thinkingLevel`, Ollama 미지원). effort를 지원하지 않는 모델에는 파라미터를 생략해 공급자 기본 샘플링이 적용됩니다.
- 웹 요약을 별도 모델 대신 현재 선택된 모델로 처리.

### Removed
- TimeBox 노트 생성 기능 및 관련 템플릿.

## [0.2.15] - 2026-06-14

- 현재 배포 베이스 버전. 이후의 변경 사항은 위 [Unreleased] 섹션을 참고하세요.
