# Graph RAG & Second Brain

[← 돌아가기: README-KR.md](../README-KR.md)

[English](second-brain-en.md) | [한국어](second-brain-kr.md) | [日本語](second-brain-ja.md)

이 가이드는 **0.7.10** 기준입니다.

이 문서는 볼트 검색(Graph RAG)과 지식 쓰기(Second Brain) 계층을 상세히 다룹니다. 설치와 기본 사용법은 README를 참고하세요.

> 커맨드 팔레트의 표시 이름은 설정에서 고른 UI 언어를 따릅니다. 아래에 인용한 이름은 한국어판입니다.
> 팔레트는 로드 시점에 캐시되므로 언어를 바꾼 뒤에는 옵시디언을 다시 열어야 합니다.

### Graph RAG 검색

노트를 청크로 나눠 임베딩하고, 검색된 노트의 링크를 따라 이웃 노트까지 후보에 포함하는 검색 계층입니다.

동작 순서:

1. **청크 임베딩** — 노트를 기본 2000자(겹침 200자) 단위로 분할해 청크마다 임베딩을 생성합니다.
2. **벡터 검색** — 쿼리 임베딩과 비교해 시드 노트를 뽑습니다.
3. **그래프 순회** — 시드 노트의 아웃링크·백링크를 따라 이웃을 확장합니다. 탐색 깊이는 0~3(기본 1)이며, 0이면 순회를 하지 않고 시드만 사용합니다.
4. **통합 점수** — 관련성(0.0~1.0으로 정규화한 벡터 유사도)에 그래프 거리 감쇠를 **곱합니다**. 감쇠 가중치는 `0.5^hop`이라 hop이 1 늘어날 때마다 절반이 되고, 결과도 0.0~1.0 범위입니다. 이웃 노트의 관련성은 "이웃 자신의 유사도 60% + 시드 유사도 40%"의 가중 평균입니다.
5. **최소 관련성 필터** — 감쇠를 적용하기 **전의** 관련성이 0.55 미만인 후보를 제외합니다. 감쇠 후 점수에 적용하면 hop 1 이웃의 이론적 최대값(1.0 × 0.5 = 0.5)이 임계값보다 낮아 그래프 이웃이 전부 탈락하기 때문입니다. 이 필터로 후보가 하나도 남지 않으면 키워드 검색으로 폴백합니다.

임베딩 시그니처는 `{프로바이더}:{모델 ID}` 형태입니다. 예: `bedrock:amazon.titan-embed-text-v2:0`, `openai:text-embedding-3-large`, `ollama:nomic-embed-text`, `gemini:text-embedding-004`

설정 → **Graph RAG 검색**:

| 설정 | 기본값 | 설명 |
|------|--------|------|
| 그래프 순회 깊이 | 1 | 링크를 따라 확장할 hop 수 (0~3, 0이면 순회 비활성) |
| 청크 최대 크기 | 2000 | 단일 청크의 최대 글자 수 |
| 청크 겹침 크기 | 200 | 인접 청크 간 겹침 글자 수 (청크 최대 크기보다 작아야 함) |

**하이브리드 검색** — 벡터 검색과 나란히 키워드 검색을 돌리고, 두 순위를 상호 순위 융합(RRF, `k = 60`)으로 합칩니다. 키워드 목록의 가중치는 벡터 목록의 절반입니다 — 이 플러그인의 주 신호는 임베딩이고, 가중치를 같게 두면 어떤 단어를 여러 번 반복한 노트가 실제로 답이 있는 노트를 밀어냅니다. 대신 키워드 검색에만 걸린 후보에게 결과 자리 2개를 예약해, 오류 코드·함수명·버전 문자열처럼 정확히 일치해야 하는 질의가 임베딩 유사도에 묻히지 않게 합니다.

**적중 본문** — 검색 결과에는 노트 앞부분이 아니라 **실제로 맞은 청크의 본문**(최대 2000자)이 담깁니다. 노트 뒷부분의 한 문단이 맞아서 올라온 결과라면, 앞 500자를 발췌해 넘길 때 모델이 정작 그 노트가 검색된 이유를 못 봅니다.

**증분 인덱싱** — 파일 생성·수정·이름변경·삭제 이벤트를 반영합니다. 2초 디바운스 후 동시 2개까지 처리하되 **같은 경로의 연속 이벤트는 순서를 지킵니다** — 생성 직후 삭제가 뒤바뀌어 적용되면 지운 노트가 검색에 남습니다. 폴더 이동처럼 여러 파일이 한꺼번에 바뀌어도 임베딩 요청이 한꺼번에 몰리지는 않습니다.

**임베딩 모델 변경 감지** — 인덱스에 `{백엔드}:{모델}` 시그니처와 벡터 차원을 함께 저장합니다. 임베딩 모델이나 백엔드를 바꾸면 기존 벡터를 폐기하고 키워드 검색으로 폴백하며, 재인덱싱이 필요하다는 안내를 표시합니다. 차원이 다른 벡터는 유사도 0이 아니라 "비교 불가"로 처리합니다. 일부만 불일치하는 경우(재인덱싱 진행 중 등)에는 비교 가능한 후보로 검색을 계속하면서 인덱스가 낡았음을 함께 알립니다.

**인덱싱 시 전송되는 데이터** — 대상 노트와 지원 텍스트 첨부를 청크로 나눠 설정한 임베딩 API에 전송합니다. 외부 엔드포인트를 쓰면 해당 본문이 기기 밖으로 나갑니다. 같은 기기에서 실행한 Ollama를 지정하면 임베딩 요청을 로컬에서 처리합니다.

### Second Brain Layer (옵트인)

Graph RAG의 "읽기" 위에 노트 생성·갱신을 올린 계층입니다.

> **기본값은 꺼져 있습니다.** 설정 → Agent LLMs → **Second Brain** → "Second Brain 활성화"를 켜기 전에는 이 계층이 어떤 노트도 자동으로 생성·수정·삭제하지 않습니다. 활성화 전에는 관련 커맨드와 도구가 안내 메시지만 반환하며, LLM 호출도 하지 않습니다.

#### 사고 도구 (노트를 만들지 않음)

읽기 전용 도구입니다. 볼트를 검색해 결과를 채팅에 보여주기만 하고, 어떤 노트도 생성·수정하지 않습니다.

| 도구 | 커맨드 | 하는 일 | LLM 호출 |
|------|--------|---------|----------|
| `challenge` | 주장 반박 (challenge) | 과거 노트를 근거로 현재 주장의 허점·반례·전제를 검토 | 1회 |
| `connect` | 두 주제 연결 (connect) | 두 주제를 각각 검색해 교차 컨텍스트로 묶고 연결 아이디어 도출 | 1회 |
| `emerge` | 최근 패턴 발견 (emerge) | 최근 N일 노트에서 아직 이름 붙지 않은 패턴 발견 | 1회 |
| `reconcile_topic` | 모순 점검 (reconcile) | 노트 간 상충하는 진술을 점검해 리포트로 제시 (노트 미변경) | 1회 |

`emerge`는 프롬프트에 넣는 노트를 최신 60건으로 제한하고, 초과분이 있으면 몇 건 중 60건만 분석했는지 함께 알립니다.

#### 생성 도구 (노트를 만듦)

| 도구 | 커맨드 | 하는 일 | LLM 호출 |
|------|--------|---------|----------|
| `synthesize_topic` | 주제 종합 (synthesize) | 주제 관련 노트를 검색해 종합 노트를 생성/갱신 | 1회 |
| `architect` | 코드베이스 아키텍트 (architect) | 코드베이스 구조를 스캔해 `Architecture.md`의 overview/modules/decisions 섹션 작성 | 3회 (섹션당 1회) |
| `create_wiki_note` | 위키 노트 생성 | 제목·본문을 받아 프론트매터 + `## For future AI` 프리앰블 규격 노트 생성 | 0회 |
| `update_index` | 위키 인덱스 갱신 | 위키 폴더 노트를 모아 `index.md` 카탈로그 갱신 | 0회 |

`synthesize_topic`은 검색 결과가 없으면 노트를 만들지 않고 안내만 반환합니다. `architect`는 볼트 파일 목록을 직접 읽고 모듈 골격을 400줄까지만 프롬프트에 넣으며, `update_index`도 볼트의 마크다운 파일을 직접 훑습니다. 즉 이 두 도구는 Graph RAG 인덱스를 사용하지 않습니다.

#### Sentinel 블록 — 내가 적은 메모는 보존됩니다

생성 도구는 만든 영역을 마커로 감쌉니다.

```markdown
<!-- @generated:synthesis -->
(AI가 생성한 내용)
<!-- @end:synthesis -->

여기에 직접 적은 메모는 재생성해도 그대로 남습니다.
```

재실행하면 해당 블록 **안쪽만** 교체하므로, 같은 노트에 직접 적어둔 메모·프론트매터·프리앰블은 그대로 남습니다(멱등 갱신). LLM 응답에 마커 문자열이 섞여 들어오면 블록 구조가 깨지지 않도록 무해화합니다.

블록 키는 기능별로 나뉩니다: `synthesis`(주제 종합), `overview`/`modules`/`decisions`(아키텍처), `catalog`(위키 인덱스), `knowledge-gaps`(지식 공백 리포트), `reconcile`(모순 검토), `related-links`(링크 제안), `canonical-candidates`(중복 후보), `decisions`(결정 원장).

#### 그 밖의 안전 장치

- **위키 폴더 밖 쓰기 차단** — 경로에 `..`, 절대경로, 드라이브 문자가 포함되면 거부합니다.
- **덮어쓰기 금지** — `create_wiki_note`와 대화 결론 수확은 같은 경로에 파일이 이미 있으면 덮어쓰지 않고 실패로 보고합니다.
- **도구 실행 확인** — 설정 → Agent LLMs → 대화 → **노트 변경·MCP 도구 실행 확인**(기본 꺼짐)을 켜면 노트를 생성·편집·삭제·이동하는 도구(`create_wiki_note`, `update_index`, `synthesize_topic`, `architect` 포함)와 모든 외부 MCP 도구 실행 전에 확인 대화상자를 표시합니다.
- **낡은 인덱스 경고** — 임베딩 구성이 바뀐 상태에서 결과를 만들면 결과 메시지에 경고 문구를 덧붙입니다.

#### 설정

설정 → **Second Brain**:

| 설정 | 기본값 | 설명 |
|------|--------|------|
| Second Brain 활성화 | 꺼짐 | 이 계층 전체의 옵트인 스위치 |
| 위키 폴더 | `Second Brain` | 생성 노트의 루트 폴더. 하위에 `entities`, `concepts`, `projects` 폴더를 만듭니다 |
| 스케줄러 활성화 | 꺼짐 | 시작 시와 이후 30분마다 조건을 확인하고, 설정 주기가 지났으면 정리 실행 |
| 스케줄러 주기 (시간) | 24 | 자동 실행 간 최소 시간 (최소 1) |

#### 스케줄러 (정리 파이프라인)

Second Brain과 스케줄러를 모두 켜면 **Obsidian 시작 시와 이후 30분마다** 실행 조건을 확인합니다. 마지막 실행에서 설정 주기(기본 **24시간**)가 지나야 정리를 실행하므로, 조건 확인 간격과 실제 실행 간격은 다릅니다. Obsidian을 닫으면 실행되지 않습니다.

Second Brain이 켜져 있으면 스케줄러 토글이 꺼져 있어도 커맨드 **"Second Brain 정리 실행 (스케줄러)"** 로 즉시 실행할 수 있습니다. 종합·모순 반영은 파이프라인에 포함되지 않으며, 생성 영역과 로그만 갱신합니다. **LLM 호출 0회.**

1. 위키 폴더와 카테고리 폴더 보장 (없으면 생성, 있으면 유지)
2. `index.md` 카탈로그 갱신 (`catalog` 블록만 교체)
3. 지식 공백 리포트 갱신
4. Bases 대시보드 갱신 (생성 표식이 없는 파일은 덮어쓰지 않음)
5. `log.md`에 실행 이력 한 줄 append (기존 로그는 변경하지 않음)

한 단계가 실패해도 나머지 단계는 계속 실행됩니다. 모든 단계가 실패하면 마지막 실행 시각을 갱신하지 않습니다. 같은 앱 세션의 자동 재시도에는 **1시간 냉각**을 적용하고, 이후 실행 조건을 만족하는 다음 확인 시점에 다시 시도합니다.

### 승인형 정리

다섯 개 커맨드는 변경안을 제시하고 멈춥니다. 항목별로 체크하는 목록이 열리고, 승인하기 전에는 아무것도 쓰지 않습니다. 승인한 내용도 sentinel 블록 안에 들어가므로 같은 파일에 직접 적은 글은 그대로 남습니다.

| 커맨드 | 무엇을 제안하는가 | 반영 시 LLM 호출 |
|--------|------------------|------------------|
| 모순 검토 및 반영 (reconcile → apply) | 한 주제의 노트들이 서로 어긋나는 진술에 대한 정정안 | 0 — 점검을 이미 했습니다 |
| 링크 제안 (고아·스텁 노트 연결) | 고아·스텁 노트에서 관련 노트로 가는 링크 | 0 |
| 중복 후보 검토 (정본·별칭 정리) | 같은 대상을 다루는 노트들의 정본과 별칭 | 0 |
| 결정 추출 → 원장 (decisions) | 이유·담당·기한·근거가 붙은 결정 | 0 — 추출을 이미 했습니다 |
| Inbox 검토 (제목·이동·태그 제안) | 새로 캡처한 노트의 제목·대상 폴더·태그 | 0 — 제안을 이미 했습니다 |

**링크 제안**은 유사도 0.82 이상만 후보로 삼고, 노트당 최대 3개를 제안합니다. 승인한 링크는 `related-links` 블록에 붙고, 다시 실행하면 그 블록을 교체하므로 중복이 쌓이지 않습니다.

**중복 후보**는 유사도 0.9 이상, 한 군집 최대 8개입니다. **아무것도 지우거나 합치지 않습니다.** 정본 노트의 프론트매터에 `aliases`를 넣고 `canonical-candidates` 블록에 나머지를 나열할 뿐이라, 합치는 판단(또는 "중복이 아니었다"는 판단)은 사용자가 합니다.

**결정 원장**은 `{위키 폴더}/Decisions.md`입니다. 병합할 때 사용자가 직접 고친 값은 유지하고 빈 칸만 채우는데, 상태는 예외입니다 — `open` → `done` → `superseded` 방향으로만 움직이고 되돌아가지 않습니다. 대체된 결정은 지우지 않고 표시만 합니다. 한때 다르게 결정했다는 사실 자체가 남길 가치가 있기 때문입니다.

**Inbox 검토**는 한 번에 최대 12개 노트를 다룹니다. 이름 변경은 옵시디언의 링크 보존 API를 쓰므로 기존 이름을 가리키던 링크가 파일을 따라갑니다. 대상 이름이 이미 있으면 이동은 건너뛰고 태그는 그대로 적용합니다.

**비용:** 반영은 모두 LLM 호출 0회입니다. 제안을 만드는 쪽은 모순·결정·Inbox 검토가 각각 1회, 링크 제안과 중복 후보는 0회입니다(인덱스에 저장된 임베딩만 씁니다).

### 인용 검증

답변이 노트를 인용하면 그 경로를 인덱스와, `#헤딩` 앵커를 그 노트의 헤딩 목록과 대조합니다. 확인되지 않은 인용은 답변 아래에 나열됩니다.

- 경로는 옵시디언이 링크를 해석하는 방식과 같게 **경로 접미사**로 맞춥니다 — `Projects/Note`를 인용하면 `Archive/Projects/Note.md`가 맞습니다.
- 코드 스팬과 코드 블록은 건너뜁니다. 예제 안의 경로를 인용으로 세지 않습니다.
- 외부 링크(URI 스킴이 있는 것, 프로토콜 상대 URL, 프래그먼트만 있는 것)는 무시합니다.
- 복원한 메시지에도 검증이 돌아갑니다. 사이드바를 다시 열거나 저장한 세션을 불러와도 같은 경고가 보입니다.

모델이 그럴듯한 경로를 지어내는 특정 실패를 잡기 위한 장치입니다. 답변 자체는 그대로 보여주고 표시만 합니다.

**비용:** LLM 호출 0회, 임베딩 호출 0회.

### 노트 링크 표기

플러그인이 쓰는 모든 링크는 한 함수를 거칩니다. 쓰는 곳이 여러 곳이라도 규칙이 같습니다.

- 링크 대상은 확장자를 뗀 **경로**이고, 노트 제목은 별칭으로 붙습니다: `[[경로/노트|제목]]`. 제목을 링크 대상으로 쓰지 않습니다 — 노트의 제목은 본문 첫 `# H1`에서 오고, 파일명과 다른 경우가 흔합니다.
- 위키링크 안에서 `#`와 `|`는 이스케이프할 방법이 없습니다. `#`는 앵커를 시작하고 첫 `|`는 별칭을 시작합니다. 경로나 별칭에 이 문자가 있으면 퍼센트 인코딩한 마크다운 링크로 물러납니다. 옵시디언은 두 형태를 같게 해석합니다.
- 앵커 링크는 헤딩을 앵커로 쓸 수 있을 때만 씁니다. 그렇지 않으면 노트 전체를 가리키는 인용으로 물러납니다.

### 프론트매터 속성 검색

폴더·태그·기간 외에, 프론트매터의 아무 속성으로도 검색을 걸러낼 수 있습니다.

| 연산자 | 의미 |
|--------|------|
| `=` / `!=` | 같음 / 다름 |
| `>` `>=` `<` `<=` | 숫자·날짜 비교 |
| `~` | 부분 포함 |

점 표기법으로 중첩 키까지 갑니다 — `project.status = active`는 `project: { status: active }`에 맞습니다.

잘못된 조건은 **버리지 않고 알립니다.** 모델이 `modifiedAfter: "지난달"`처럼 넘겼을 때 조건이 무시된 채 볼트 전체 결과를 받으면, 모델도 사용자도 조건이 적용되지 않았다는 사실을 모릅니다.

날짜는 **로컬 시간** 기준으로 해석합니다. `new Date("2026-09-01")`은 UTC 자정이라 KST에서는 9월 1일 09:00이 되고, 그러면 그날 새벽에 쓴 노트가 "9월 1일 이후" 조건에서 빠집니다. 인덱스의 수정 시각이 로컬 파일 시각이므로 여기서도 맞췄습니다.

**비용:** LLM·임베딩 호출 없음 — 인덱스 데이터만 씁니다.

### AI 변경 원장과 안전한 되돌리기

노트를 바꾼 AI 작업마다 변경 전·후 스냅샷을 기록합니다. 최근 20건을 보관합니다.

- **되돌리기는 그 이후 사용자가 손대지 않은 파일만 복원합니다.** 손댔으면 덮어쓰지 않고 건너뜁니다 — 사용자의 편집이 되돌리기 때문에 사라지는 일은 없습니다.
- 생성·수정·삭제된 경로를 모두 담고, 폴더도 포함합니다.
- 복원 전에 경로를 검사합니다. 절대 경로, `..` 탈출, 빈 세그먼트는 거부합니다.
- 원장은 볼트가 아니라 플러그인 폴더에 저장됩니다.

**AI 변경 원장 열기**로 목록을 보고, **마지막 AI 변경 되돌리기**로 직전 작업을 되돌립니다.

**비용:** LLM 호출 0회, 임베딩 호출 0회.

### 첨부 파일 RAG

볼트의 `.txt`·`.csv`·`.json`·`.html` 파일을 청크로 색인해 노트와 함께 검색합니다. 대화 한 번에 읽고 버리는 방식이 아닙니다.

노트와 같은 파이프라인을 탑니다 — 청크 임베딩, 키워드 융합, 링크가 있으면 그래프 순회까지. 재색인도 같은 파일 이벤트를 따릅니다.

### 출처 청크 해시·오래됨 표시·재생성 diff

종합 노트는 출처 경로만이 아니라 **출처 청크마다 내용 해시**를 기록합니다.

- 출처 내용이 바뀌면 해시가 어긋나 노트에 오래됨 표시가 붙습니다. 경로가 그대로 있다는 것만으로 종합이 최신이라고 볼 수 없습니다.
- 다시 생성할 때 무엇이 달라졌는지 **diff**(최대 40줄)로 보여줍니다. 갱신이 실질적인지 보고 받아들일 수 있습니다.
- 출처 기록은 `synthesis-provenance` 센티널 블록에 따로 들어갑니다. 재생성 시 그 블록만 교체되고 사용자가 쓴 글은 남습니다.

### Bases 대시보드

볼트 데이터로 뷰 4개를 담은 `.base` 파일을 만듭니다.

| 뷰 | 내용 |
|----|------|
| 결정 원장 | 상태·담당·기한이 붙은 결정 |
| 미해결 질문 | 기록됐지만 아직 답이 없는 질문 |
| 오래된 지식 | 종합 이후 출처가 바뀐 노트 |
| 복습 큐 | 연결은 많지만 오래 열지 않은 노트 |

**Agent LLMs 대시보드 갱신·열기** 커맨드로 실행하거나, 스케줄러 파이프라인이 갱신합니다.

- 생성 파일에는 표식이 붙습니다. **표식이 없는 파일은 절대 덮어쓰지 않습니다** — 사용자가 쓴 파일과 경로가 겹치면 조용히 지우는 대신 실패를 알립니다.
- 사라진 투영은 **삭제하지 않고 비활성화**합니다(`dashboard_active: false`). 생성 파일에 사용자가 메모를 더했더라도 남습니다.

**비용:** LLM 호출 0회, 임베딩 호출 0회.

### 지식 공백 리포트

인덱스 데이터로 구조적 공백을 로컬 계산합니다. **LLM 호출 0회.** 커맨드 **"지식 공백 리포트 갱신"** 또는 스케줄러 파이프라인으로 실행합니다.

네 가지 지표:

| 지표 | 판정 기준 |
|------|-----------|
| 참조되지만 없는 노트 | 다른 노트가 링크하는데 아직 존재하지 않음 |
| 참조는 받는데 내용이 부족한 노트 | 백링크가 있고 본문이 200자 미만 |
| 어디에도 연결되지 않은 노트 | 아웃링크·백링크 모두 없음 (그래프 순회로 도달 불가) |
| 한 방향으로만 연결된 노트 | 링크가 한쪽 방향만 존재 |

상위 20건을 `{위키 폴더}/Knowledge Gaps.md`에 sentinel 블록으로 기록합니다. 자동 수정은 하지 않고 "볼 곳"만 위키링크로 제시하므로, 아직 없는 노트는 클릭해서 바로 만들 수 있습니다. 위키 폴더 하위의 생성 노트는 집계에서 제외됩니다.

### 복습 큐

오래 열지 않았지만 연결이 많은 노트 5건을 제시합니다. **LLM 호출 0회.** 커맨드 **"복습 큐 (다시 볼 노트)"**.

- 점수 = 경과 일수 점수 + 링크 수 점수
- 본문 200자 미만 노트는 제외합니다 (빈 노트는 지식 공백 리포트가 다룹니다)
- 최근 7일 내에 이미 제시한 노트는 제외해 며칠 연속 같은 노트가 나오지 않게 합니다
- 노트에 등급 필드를 심지 않습니다. 열람 이력은 플러그인 설정에만 저장합니다
- 접근 이력이 없는 도입 초기에는 노트 수정 시각으로 폴백합니다
- 삭제·이동된 노트는 이력에서 정리됩니다

### 대화 결론 수확

지난 대화 목록에서 새싹 아이콘을 누르면 그 대화의 결론·결정·근거·미해결 질문만 추출해 `{위키 폴더}/Conversations/YYYY-MM-DD 제목.md`로 저장합니다. **수확 1회당 LLM 1회.** Second Brain 활성화가 필요합니다.

대화 세션은 최대 50개까지만 보관되므로 오래된 것은 조용히 사라집니다. 남길 가치가 있는 결론은 노트로 옮겨두면 인덱싱되어 이후 검색 근거가 됩니다.

원본 대화는 노트에 넣지 않습니다. 잡담과 시행착오까지 검색 근거가 되면 이후 검색 품질이 떨어지기 때문입니다. 자동 실행은 없고 수동 트리거만 제공하며, 같은 이름의 노트가 이미 있으면 덮어쓰지 않습니다.

### 회고 체인

일일 회고는 채팅에 **"회고" / "retrospective" / "振り返り"** 를 입력하면 생성됩니다(전용 버튼은 없습니다). 이때 최근 7일의 회고 섹션을 함께 입력해 반복되는 문제와 개선 여부를 추적합니다. **LLM 호출은 늘어나지 않습니다** (기존과 동일하게 회고 생성 1회).

일일 노트 전체가 아니라 회고 섹션만, 건당 1000자까지 넣습니다. 과거 회고가 없으면 기존과 동일하게 당일 입력만으로 동작합니다.
